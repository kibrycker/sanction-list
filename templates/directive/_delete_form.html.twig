<form method="post" action="{{ path('sanction_list_admin_directive_delete', {'id': directive.id}) }}"
      onsubmit="return confirm('Вы уверены, что хотите удалить директиву?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ directive.id) }}">
    <button class="btn btn-danger">Удалить</button>
</form>
